--- a/lib/easy_lock.h
+++ b/lib/easy_lock.h
@@ -76,6 +76,10 @@
 
 #endif
 
+#if defined(HAVE_SCHED_YIELD)
+#include <sched.h>
+#endif
+
 static inline void curl_simple_lock_lock(curl_simple_lock *lock)
 {
   for(;;) {
