--- a/deps/lua/src/Makefile
+++ b/deps/lua/src/Makefile
@@ -8,11 +8,11 @@
 PLAT= none
 
 CC?= gcc
-CFLAGS= -O2 -Wall $(MYCFLAGS)
-AR= ar rcu
-RANLIB= ranlib
-RM= rm -f
-LIBS= -lm $(MYLIBS)
+CFLAGS?= -O2 -Wall $(MYCFLAGS)
+AR?= ar rcu
+RANLIB?= ranlib
+RM?= rm -f
+LIBS?= -lm $(MYLIBS)
 
 MYCFLAGS=
 MYLDFLAGS=
